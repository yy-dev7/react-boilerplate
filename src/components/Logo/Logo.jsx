import React, { PropTypes, Component } from 'react'
import './Logo.scss'

export class Logo extends Component {
  static propTypes = {

  };

  state = {

  };

  render() {
    return (
      <div>Logo</div>
    )
  }
}

export default Logo
